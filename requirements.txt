# Runtime dependencies for the hosted demo.
# Local development uses pyproject.toml: pip install -e ".[dev,app]"
#
# torch, torchvision and torchaudio are NOT listed here. The Dockerfile installs all three
# together from the CPU-only index: the PyPI wheels bundle CUDA libraries a CPU instance
# cannot use, and the three must come from one resolve or their C++ extensions do not match.
av>=13
numpy>=1.26
pandas>=2.0
transformers>=4.40,<5
streamlit>=1.40
altair>=5
muq
openai-whisper
