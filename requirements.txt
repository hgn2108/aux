# Runtime dependencies for the hosted demo (Hugging Face Spaces reads this file).
# Local development uses pyproject.toml: pip install -e ".[dev,app]"
av>=13
numpy>=1.26
torch>=2.2
transformers>=4.40,<5
streamlit>=1.40
pandas>=2.0
altair>=5
openai-whisper
muq
