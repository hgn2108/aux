# Runtime dependencies for the hosted demo.
# Local development uses pyproject.toml: pip install -e ".[dev,app]"
#
# torch and torchaudio are NOT listed here. The Dockerfile installs them first from the
# CPU-only index, because the PyPI wheels bundle CUDA libraries a CPU instance cannot use
# and that no image-size budget survives.
av>=13
numpy>=1.26
pandas>=2.0
transformers>=4.40,<5
streamlit>=1.40
altair>=5
muq
openai-whisper
